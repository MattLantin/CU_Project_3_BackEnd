/*** Scriot to handel getting and processing data from the backend* @author Nathan Stevens* @version 1.0*/// set the root urlvar root = location.protocol + '//' + location.host;console.log("Root", root)// capture button eventsd3.selectAll("button").on("click", function() {    // Create a variable for the button selected    let button = d3.select(this);            let value = button.property("value");    console.log("Button Value:", value);        if(value == "update_map") {      updateMap();    } else if(value == "plot_data") {      console.log("plot data ...");    } else {      loadDRGDefinition(value);    }});// function to update the mapfunction updateMap() {  let value = d3.select("#drg_option").property("value");  let drgText = d3.select("#drg_option option:checked").text()    console.log("DRG:", drgText, value);    // let user know what drg was selected  d3.select("#drg").text(drgText);    // call function to create a new map  let url = root + "/api/v1.0/drg/" + value;    d3.json(url).then(function (data) {    console.log(data)        let min = 100000000;    let minState = "MIN";    let max = 0;    let maxState = "MAX";           for (let k in data) {      item = data[k];            if(item.avg_payments < min) {        min = item.avg_payments;        minState = item.state;      }            if(item.avg_payments > max) {        max = item.avg_payments;        maxState = item.state;      }     }        d3.select("#lowest").text(minState + " ($" + min.toLocaleString() + ")");    d3.select("#highest").text(maxState + " ($" + max.toLocaleString() + ")");        // create the map with cost for that particular procedure    createMapWithCost(data, min, max);   });}// us d3 to load the difinitions from the backendfunction loadDRGDefinition(category) {  let url = root + "/api/v1.0/definitions/" + category  console.log("loading drg definition: ", url);    // Perform a GET request to the query URL.  d3.json(url).then(function (data) {    // clear the select drop down    let dropDown = d3.select("#drg_option");    //dropDown.html(""); // clear it        let htmlText = "";    data.forEach(item => {      htmlText += "<option value=\"" + item.value + "\">" + item.text + "</option>";    });    dropDown.html(htmlText);  });}